<?xml version="1.0" encoding="UTF-8"?>

<output>
    <ticket>
                <line/>	
                <line>
		    <text align="center" length="40" >${empresa.getNombreComercial()}</text>
		</line>
		<line>
		    <text align="center" length="40" >${tienda.getDesFormato()}</text>
		</line>
		#foreach ($linea in $empresa.getDireccionEmpresaAsLineas().getLineas())
			<line><text align="left" length="40" >${linea}</text> </line>
		#end
		
		#foreach ($lineaDL in $tienda.getDireccionLocalAsLineas().getLineas())
			<line><text align="left" length="40" >${lineaDL}</text> </line>
		#end
		<line/>		
		<line>
			<text align="center" length="40" > ${fecha} ${hora} </text>
		</line>
		<line>
		<text align="center" length="40" >-------------------------------</text>
		</line>
		<line>
		#foreach ($lineaT in $tipoDocumento.getLineas())
			<line><text align="center" length="40" >${lineaT}</text> </line>
		#end	
		</line>		
		<line>
		<text align="center" length="40" >${idDocumento}</text>
        </line>
		<line/>
		<line>
		<text align="left" length = "8">TOTAL:$ </text><text align="left" length = "13">${totalPagado}</text>
		</line>
		<line/>
		#foreach ($pago in $pagos)
			#foreach ($lineaP in $pago.getDesMedioPagoAnulacionAsLineas().getLineas())
				<line><text align="left" length="40" >${lineaP}</text> </line>
			#end
		<line>
			<text align="left" length = "9">Valor: $ </text>
			<text align="left" length = "13">${pago.getUstedPagaPrint().toString()}</text>	
		</line>
			#if (${pago.isHayImporteInteresAnulacion()})
			<line>
				<text align="left" length = "11">Interes: $ </text>
				<text align="left" length = "13">${pago.getImporteInteresAnulacion().toString()}</text>				
			</line>	
			<line/>
			#else
				<line/>
			#end	
		#end
		<line/>
		<line>
			<text align="left" length="40" >Cajero: ${cajero}</text>
        </line>
		<line>
			<text align="left" length="40" >Administrador: ${autorizador}</text>
        </line>			
		<line></line>
		<line></line>
		<line/>
		<line/>
		<line/>
		<line/>
		<line/>
		<line/>
		<line/>
		<line/>
		<line/>
		<line/>
		<line/>
 
    </ticket>
</output>